export class ProductCategory {
  // have already configured backend repository to expose entity ids
  // check out backend API http://localhost:8080/api/product-category
  id: number;
  categoryName: string;
}